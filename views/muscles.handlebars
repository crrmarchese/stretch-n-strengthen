<section class="section">

    <div class="container">
      <h1 class="title is-size-1">Muscles</h1>
        <div class="columns is-mobile is-centered">
            <div class="column is-half">
                <div class="test"> 
                     <ul class="ss-list-group">
                        {{#each muscles as |muscle|}}
                        {{> muscles-details}}
                        {{/each}}
                    </ul>

                </div>
               
            </div>
        </div>
    </div>
  
</section>  
