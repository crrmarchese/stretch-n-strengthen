<section class="section">

    <div class="container">
        <div class="columns is-mobile is-centered">
            <div class="column column-custom-center">
                 <h1 class="title is-size-1 has-text-centered">Muscles</h1>
                     <ul class="ss-list-group">
                        {{#each muscles as |muscle|}}
                        {{> muscles-details}}
                        {{/each}}
                    </ul>
            </div>
        </div>
    </div>
  
</section> 
 
